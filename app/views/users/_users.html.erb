<% if users.any? %>
<ul class="list-unstyled">
    <% users.each do |user| %>
    <li class="media">
       <div class="media-body">
           <div>
               
               <div class="picture-container"><img class="picture mr-2" src="<%= user.image.thumb100 %>" alt=""></div>
               
           </div>
           <div>
               <%= user.name %>
           </div>
           <div>
               <p><%= link_to 'ユーザー詳細', user_path(user) %></p>
           </div>
       </div>
     </li>
    <% end %>
</ul>
<%= paginate users %>
<% end %>
